apiVersion: v1
kind: Service
metadata:
  name: voyage-analytics-service
spec:
  type: LoadBalancer
  selector:
    app: voyage-analytics
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000